-- Messenger
-- 1) Message (→ Mongo DB)
-- hitea_msg (
--  no number, 
--  sendTo string, 
--  sendFrom string, 
--  txt string, 
--  date date,
--  sfDel boolean, 
--  stDel boolean, 
--  stCheck boolean
-- );

-- 2) Messenger List (→ Oracle DB)
create table hitea_messenger_list(
  hmsl_no number(10) primary key, 
  hmsl_m1 varchar2(20 char) not null, 
  hmsl_m2 varcahr2(20 char) not null,
  hmsl_last_txt1 varchar2(300 char) not null, 
  hmsl_last_txt2 varchar2(300 char) not null, 
  hmsl_last_date date not null
);

create sequece hitea_messenger_list_seq


// 참고
ORA-00907 에러나면 복사하여 붙여 넣지 말고 아예 새로 타이핑하면 해결됨
